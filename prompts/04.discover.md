1. Type Declarations
File: types/meal.ts
Declare the exact structure for mapping raw fields from the TheMealDB API into clean TypeScript contract shapes:

TypeScript
export type MealListItem = {
  idMeal: string;
  strMeal: string;
  strMealThumb: string;
  strCategory?: string;
  strArea?: string;
};

export type MealListResponse = {
  meals: MealListItem[] | null;
};
2. API Services Configuration
File: app/services/mealApi.ts
Implement a network request wrapper module targeting [https://www.themealdb.com/api/json/v1/1/](https://www.themealdb.com/api/json/v1/1/) with the following explicit logic functions:

Static Configurations:

Export an array containing these specific category tokens: ['Chicken', 'Dessert', 'Miscellaneous', 'Pasta', 'Seafood', 'Side', 'Vegan', 'Vegetarian', 'Breakfast'].

getMealByCategory(category: string): Promise<MealListResponse>

Target Endpoint: filter.php?c=${category}

getMealByName(query: string): Promise<MealListResponse>

Target Endpoint Logic: If query.length === 1, treat it as a single-character lookahead and route to search.php?f=${query}. Otherwise, perform a full-string search using search.php?s=${query}.

3. UI Component Screen Logic
File: app/(tabs)/index.tsx
Create the functional layout framework matching these structured layout sections:

A. Core State Hooks & Callbacks
const [selectedCategory, setSelectedCategory] = useState('Chicken');

const [searchQuery, setSearchQuery] = useState('');

const [meals, setMeals] = useState<MealListItem[]>([]);

const [loading, setLoading] = useState(false);

Derived State: const isSearching = searchQuery.trim().length > 0;

Fetch Effect Callback (loadMeals): Wrap inside an explicit handler triggered whenever selectedCategory or searchQuery updates. If isSearching evaluates to true, call getMealByName(searchQuery), else trigger getMealByCategory(selectedCategory). Handle empty/null array evaluations safely.

B. Header & Navigation Structure
Enclose the screen inside a root SafeAreaView.

Back Button Action Row: A row aligned container containing a Pressable.

Visuals: Features a ChevronLeft icon horizontally preceding the text "Back".

Interaction Logic: Triggers a light tactile feedback pulse (Haptics.impactAsync). Uses router.canGoBack() to call router.back(), falling back to router.replace('/') if no page depth exists.

C. Main Layout Scrollable Canvas
Wrap everything below the header inside a smooth vertical <ScrollView className="flex-1">.

Primary Typography Title: Render an H1 heading with bold text displaying "Discoverable Meals".

Categories Horizontal Container: A horizontal slider container (<ScrollView horizontal showsHorizontalScrollIndicator="{false}">).

Map through the static collection list.

Apply highlighted styling variables (e.g., matching text/border highlights) when a card item matches selectedCategory.

Click Action: Triggers an explicit haptic pulse, updates the state container value, and triggers the filter execution loop.

Text Entry Search Area: A high-contrast layout card block matching these native settings:

Styled as a white rectangular element displaying a magnifying glass Search icon from lucide-react-native.

Includes a modern text input field utilizing the placeholder string: "Search meals by Name".

Strict Native Constraints: Force autoCorrect={false} and returnKeyType="search".

D. Conditional State Renderer Matrix
Evaluate state properties down this single-path matrix branch before mounting data collections:

Plaintext
├── if (loading === true)
│   ├── if (isSearching === true)  => Display: "Searching Meals..."
│   └── else                       => Display: "Loading meals..."
└── if (loading === false)
    ├── if (meals.length === 0)    => Display: "No meals found"
    └── if (meals.length > 0)       => Render Card Grid Map Component
E. Grid Card Layout Specifications
Map out arrays inside a structured grid template using clean component containers:

Geometry: A uniform rectangular container card designed with fixed aspect ratios, sleek rounded corner frames, clean perimeter borders, and deep shadow offsets.

Layout Blocks: The upper 50% split window contains the product graphic strMealThumb featuring overflow: 'hidden'. The lower section houses metadata details parsing strMeal alongside strCategory.

The Dynamic Save Button Container: Anchored entirely along the bottom segment extending flush to card padding lines. Styled in an Emerald Green hue with bold White center typography.

Authentication Guard Button Content Conditions:

If auth is not ready (!isLoaded): Render the label text "Loading...".

If the user is authenticated (isSignedIn === true): Render the text label "Save meal".

If the user is unauthenticated (isSignedIn === false): Still render "Save meal", but intercept the click action to trigger a medium haptic pulse and cleanly route the viewport using router.push('/sign-in').