# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

# Implementation Planning Rule

Always generate and present a structured implementation plan (`implementation_plan.md` artifact) to the user for feedback and approval before writing any code or executing modifying commands.

